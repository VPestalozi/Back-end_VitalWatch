services:
  db:
    image: postgres:latest
    container_name: vital_postgres
    restart: always
    environment:
      POSTGRES_USER: user_vital
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: pwa_database
    ports:
      - "5432:5432"
    volumes:
      - ./init:/docker-entrypoint-initdb.d